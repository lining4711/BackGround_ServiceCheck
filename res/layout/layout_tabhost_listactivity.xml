<?xml version="1.0" encoding="utf-8"?>
<TabHost xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@android:id/tabhost"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" >

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:orientation="vertical" >

        <FrameLayout
            android:id="@android:id/tabcontent"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:layout_weight="1.0"
            android:paddingBottom="53px" />

        <TabWidget
            android:id="@android:id/tabs"
            android:layout_width="fill_parent"
            android:layout_height="50px"
            android:layout_alignParentBottom="true"
            android:layout_weight="0.0"
            android:visibility="gone" />

        <RadioGroup
            android:id="@+id/main_radio"
            android:layout_width="fill_parent"
            android:layout_height="50dip"
            android:layout_gravity="bottom"
            android:background="@drawable/radiogroup_background"
            android:gravity="center_vertical"
            android:orientation="horizontal" >

            <RadioButton
                android:id="@+id/main_index_button"
                android:layout_marginRight="5dip"
                android:layout_marginTop="1.0dip"
                android:background="@drawable/blue_btn"
                android:drawableTop="@drawable/btn_basic_pre"
                android:text="miantext" />

            <RadioButton
                android:id="@+id/main_running_button"
                android:layout_marginRight="5dip"
                android:layout_marginTop="1.0dip"
                android:background="@drawable/blue_btn"
                android:drawableTop="@drawable/btn_basic_pre"
                android:text="runningtext" />

            <RadioButton
                android:id="@+id/main_uninstall_button"
                android:layout_marginTop="1.0dip"
                android:background="@drawable/blue_btn"
                android:drawableTop="@drawable/btn_basic_pre"
                android:text="uninstalltext" />
        </RadioGroup>
    </LinearLayout>

</TabHost>